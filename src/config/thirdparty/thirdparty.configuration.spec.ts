import thirdPartyConfiguration from './thirdparty.configuration';

describe('Thirdparty.Configuration', () => {
  it('should be defined', () => {
    expect(thirdPartyConfiguration).toBeDefined();
  });
});
